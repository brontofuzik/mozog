using NeuralNetwork.Construction;
using NeuralNetwork.Interfaces;

namespace NeuralNetwork.MultilayerPerceptron.Backpropagation
{
    /* TODO Remove
    public class BackpropagationNetworkBuilder : NetworkBuilder
    {
        public override INetwork MakeNetwork(NetworkArchitecture architecture) => new BackpropagationNetwork(architecture);
    }
    */
}
